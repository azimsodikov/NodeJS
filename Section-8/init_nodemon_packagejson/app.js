/**
 * * '^' means update the package to latest if it is patch or minor release
 * * '~' means update if there is a patch release
 */
var moment = require('moment');
console.log(moment().format("ddd, hA"));