/* This is the module that is gonna be seperate from the other parts of the application and can be resued on the other apps as well */
var greet = function() { 
    console.log('hello!');
};

module.exports = greet;